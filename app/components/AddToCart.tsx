"use client";

import React from "react";

const AddToCart = () => {
  return (
    <div>
      <button
        className="btn btn-secondary"
        onClick={() => {
          console.log("product page ");
        }}
      >
        CLICK ME
      </button>
    </div>
  );
};

export default AddToCart;
